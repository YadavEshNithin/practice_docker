apiVersion: v1
kind: Pod
metadata:
  name: pod-secret
  labels:
    name: pod-secret-demo
    purpose: secrets-demo
spec: 
  containers:
    - name: nginx
      image: nginx
      envFrom:
      - secretRef:
          name: pod-secret
      # - configMapRef: 
      #     name: configmap-pod-ref
            
apiVersion: v1
kind: Pod
metadata:
  name: pod-secret
  labels:
    name: pod-secret-demo
    purpose: secrets-demo
spec: 
  containers:
    - name: nginx
      image: nginx
      secretFrom:
      - secretRef:
          name: pod-secret
            